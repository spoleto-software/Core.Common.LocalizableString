# Run with job if LICENSE was changed.
name: Update LICENSE

on:
  push:
    branches: [ "main" ]
    paths:
      - 'LICENSE'
  pull_request:
    branches: [ main ]
      
jobs:
 LICENSE:
    runs-on: ubuntu-latest
    steps:
      - run: echo 'LICENSE was changed'
